# Client

## Requirements
Since this app is implemented using a release candidate version of Angular CLI please make sure that you have following versions
* Node: 24.x
* Angular CLI: 20.x

### Install node

If you have not installed nodejs or the node package manager, install it e.g. using homebrew:

## Code scaffolding

Angular CLI includes powerful code scaffolding tools. To generate a new component, run:

```bash
ng generate component component-name
```

For a complete list of available schematics (such as `components`, `directives`, or `pipes`), run:

```bash
ng generate --help
```
